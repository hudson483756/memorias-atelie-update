<UserControl x:Class="MemoriasAtelie.TelaConsultaEncomendas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Border Background="#FFF9FB" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" CornerRadius="15" Padding="15" Margin="0,0,0,15">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Central de Consultas e Relatórios de Produção" FontSize="18" FontWeight="Bold" Foreground="#444444" Margin="0,0,0,15"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="110"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="1.2*"/>
                            <ColumnDefinition Width="1.2*"/>
                            <ColumnDefinition Width="1.1*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Mês:" FontSize="11" Foreground="#777777" Margin="0,0,0,4" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CboFiltroMes" Height="32" VerticalContentAlignment="Center">
                                <ComboBoxItem Content="Todos" IsSelected="True"/>
                                <ComboBoxItem Content="Janeiro" Tag="01"/>
                                <ComboBoxItem Content="Fevereiro" Tag="02"/>
                                <ComboBoxItem Content="Março" Tag="03"/>
                                <ComboBoxItem Content="Abril" Tag="04"/>
                                <ComboBoxItem Content="Maio" Tag="05"/>
                                <ComboBoxItem Content="Junho" Tag="06"/>
                                <ComboBoxItem Content="Julho" Tag="07"/>
                                <ComboBoxItem Content="Agosto" Tag="08"/>
                                <ComboBoxItem Content="Setembro" Tag="09"/>
                                <ComboBoxItem Content="Outubro" Tag="10"/>
                                <ComboBoxItem Content="Novembro" Tag="11"/>
                                <ComboBoxItem Content="Dezembro" Tag="12"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                            <TextBlock Text="Ano:" FontSize="11" Foreground="#777777" Margin="0,0,0,4" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CboFiltroAno" Height="32" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="0,0,10,0">
                            <TextBlock Text="Cliente:" FontSize="11" Foreground="#777777" Margin="0,0,0,4" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CboFiltroCliente" Height="32" VerticalContentAlignment="Center" DisplayMemberPath="Nome" SelectedValuePath="Id"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" Margin="0,0,10,0">
                            <TextBlock Text="Peça / Produto:" FontSize="11" Foreground="#777777" Margin="0,0,0,4" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CboFiltroProduto" Height="32" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="4">
                            <TextBlock Text="Status:" FontSize="11" Foreground="#777777" Margin="0,0,0,4" FontWeight="SemiBold"/>
                            <ComboBox x:Name="CboFiltroStatus" Height="32" VerticalContentAlignment="Center" SelectedIndex="0">
                                <ComboBoxItem Content="Todos"/>
                                <ComboBoxItem Content="Pendente"/>
                                <ComboBoxItem Content="Em Produção"/>
                                <ComboBoxItem Content="Concluído"/>
                                <ComboBoxItem Content="Entregue"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Button Content="❌ Limpar Filtros" Width="120" Height="32" Margin="0,0,10,0" Background="#95A5A6" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="BtnLimpar_Click"/>
                        <Button Content="🔍 Filtrar Produção" Width="150" Height="32" Background="#FF4081" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="BtnFiltrar_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TabControl Grid.Row="1" Background="White" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="HeaderTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <ContentPresenter Content="{TemplateBinding Content}" Margin="10,4"/>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <TabItem Header="📊 Resumo de Peças Agrupadas" Foreground="#FF4081">
                    <Border Background="White" Padding="10" CornerRadius="0,0,15,15">
                        <DataGrid x:Name="DgAgrupado" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" 
                                  BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3EFEF" RowHeight="32" FontSize="14">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Peça / Produto (Ordem Alfabética)" Binding="{Binding Produto}" Width="*"/>
                                <DataGridTextColumn Header="Quantidade" Binding="{Binding QuantidadeTotal}" Width="150">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Faturamento Subtotal" Binding="{Binding FaturamentoFormatado}" Width="180">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="Bold"/>
                                            <Setter Property="Foreground" Value="#4CAF50"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </TabItem>

                <TabItem Header="📋 Histórico Detalhado" Foreground="#555555">
                    <Border Background="White" Padding="10" CornerRadius="0,0,15,15">

                        <DataGrid x:Name="DgResultados" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" 
          BorderThickness="0" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3EFEF" RowHeight="50" FontSize="13"
          SelectionMode="Single" MouseDoubleClick="DgResultados_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Data" Binding="{Binding DataFormatada}" Width="95"/>
                                <DataGridTextColumn Header="Cliente" Binding="{Binding NomeCliente}" Width="1.2*"/>
                                <DataGridTextColumn Header="Peça Produzida" Binding="{Binding Produto}" Width="1.2*"/>

                                <DataGridTemplateColumn Header="Miniatura" Width="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="4" ClipToBounds="True" Margin="3" BorderBrush="#EAEAEA" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Image Source="{Binding PrimeiraFoto}" Width="44" Height="44" Stretch="UniformToFill"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Valor" Binding="{Binding ValorFormatado}" Width="90">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="HorizontalAlignment" Value="Right"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="Descrição / Detalhes" Binding="{Binding Descricao}" Width="2*"/>
                            </DataGrid.Columns>
                        </DataGrid>


                    </Border>
                </TabItem>
            </TabControl>

            <Border Grid.Row="2" Background="#FFF1F5" BorderBrush="#FFE1EC" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,15,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📊 Resumo Período:" FontSize="13" FontWeight="Bold" Foreground="#444444" VerticalAlignment="Center" Margin="0,0,15,0"/>

                        <TextBlock Text="Total Peças:" FontSize="12" Foreground="#666666" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock x:Name="TxtTotalPecas" Text="0" FontSize="15" FontWeight="Bold" Foreground="#FF4081" VerticalAlignment="Center" Margin="0,0,20,0"/>

                        <TextBlock Text="Faturamento Total:" FontSize="12" Foreground="#666666" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock x:Name="TxtFaturamentoTotal" Text="R$ 0,00" FontSize="15" FontWeight="Bold" Foreground="#4CAF50" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button Content="🖼️ Ver Fotos Inspiração" Width="150" Height="30" Margin="0,0,8,0" Background="#333333" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="BtnVisualizarFotos_Click"/>
                        <Button Content="✏️ Editar / Atualizar" Width="130" Height="30" Background="#FF4081" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Click="BtnEditar_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>